#-----------------------------------------------------------------
# Each entry is a Node sub-class name, listing the attributes
# and child nodes of the class:
#   <name>*     - a child node
#   <name>**    - a sequence of child nodes
#   <name>      - an attribute
#
# Copyright (C) 2014
# License: BSD
#-----------------------------------------------------------------

Net: [is_pure, name, inputs*, outputs*, decls**, wiring*]

PortList: [ports**]

Synchroniser: [name, macros, path, ast*]
Morphism: [trigger, morph_list**, override_list**]

Morph: [split*, map_list**, join*]
MorphSplitMap: [split_map_list**, join*]
MorphMapJoin: [split*, map_join_list**]

SplitMap: [split*, map*]
MapJoin: [map*, join*]

Override: [join*, split*, sync*]

BinaryOp: [op, left*, right*]
UnaryOp:  [op, operand*]

Vertex:   [name, category, inputs, outputs]

ID: [value]
NUMBER: [value]
