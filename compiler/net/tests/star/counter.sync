synch sw2 (__input__, __bypass__ | NETOUT, __unblock__)
{
  start {

    on:
      input {
        send this => output, (act: [1]) => unblock;
      }

      bypass {
        send this => output;
      }
  }
}
